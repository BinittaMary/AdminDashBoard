import { SearchCourseTitlePipe } from './search-course-title.pipe';

describe('SearchCourseTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCourseTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
