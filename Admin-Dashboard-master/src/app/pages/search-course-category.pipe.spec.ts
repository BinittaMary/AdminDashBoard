import { SearchCourseCategoryPipe } from './search-course-category.pipe';

describe('SearchCourseCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCourseCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
